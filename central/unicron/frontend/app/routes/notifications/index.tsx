/**
 * Notifications Dashboard Page
 *
 * Shows notification history with filtering by date and client IP.
 */

import { Dashboard } from '~/features/notifier/pages';

export default function NotificationsDashboard() {
  return <Dashboard />;
}
