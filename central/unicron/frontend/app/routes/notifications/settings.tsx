/**
 * Notifications Settings Page
 *
 * AI enrichment settings: enable/disable, Ollama configuration, preprompt.
 */

import Settings from '~/features/notifier/components/settings/Settings';

export default function NotificationsSettings() {
  return <Settings />;
}
